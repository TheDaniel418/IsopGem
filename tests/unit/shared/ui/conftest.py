"""Test configuration for UI tests."""

import pytest
from PyQt6.QtWidgets import QApplication

# UI test specific fixtures can be added here
# The root conftest.py already handles the Python path and QApplication setup
